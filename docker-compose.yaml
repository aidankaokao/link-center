services:
  link-center:
    image: link-center:latest
    ports:
      - "${UI_PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
