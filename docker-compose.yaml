services:
  file-center:
    image: file-center:latest
    ports:
      - "${UI_PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
